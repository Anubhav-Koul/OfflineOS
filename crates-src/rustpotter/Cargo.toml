# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "rustpotter"
version = "2.0.1"
authors = ["Miguel Álvarez Díez <miguelwork92@gmail.com>"]
exclude = [
    ".vscode/*",
    "tests/*",
    "tools/*",
    "square-icon.png",
]
description = "An open source wakeword spotter forged in rust."
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/GiviMAD/rustpotter"

[dependencies.ciborium]
version = "0.2.0"

[dependencies.hound]
version = "3.4.0"

[dependencies.rubato]
version = "0.14.1"
default-features = false

[dependencies.rustfft]
version = "6.1.0"
default-features = false

[dependencies.serde]
version = "1.0.152"
features = ["derive"]
default-features = false

[features]
debug = []
default = ["debug"]
internals = []

# Vendored third-party code (see the workspace Cargo.toml note). Silence its
# pre-existing clippy lints so they don't clutter the fork's clippy runs — we do
# not modify upstream's source to satisfy our lints.
[lints.clippy]
all = { level = "allow", priority = -1 }
