{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the widget and dashboard windows are allowed to ask the shell for. Commands this crate defines itself are not gated here; this list only opens Tauri's own plugin surface, and it is deliberately small.",
  "windows": ["widget", "dashboard"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
