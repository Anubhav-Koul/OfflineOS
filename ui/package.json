{
  "name": "ironclaw-desktop-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Widget and dashboard for the IronClaw desktop app. Built against the serve API, not upstream's WebUI.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "solid-js": "^1.9.9"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.11",
    "vite-plugin-solid": "^2.11.9"
  }
}
